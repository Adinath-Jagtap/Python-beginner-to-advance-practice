# =============================================================================
# PYTHON PROBLEMS (4)
# =============================================================================
'''
1. Two Sum
   - Given a list `nums` and a target `t`, return indices (i, j) of two numbers such that nums[i] + nums[j] == t.

2. Remove duplicates from sorted array (in-place)
   - Given a sorted list `nums`, remove duplicates in-place so each element appears once and return the new length.

3. Valid palindrome (ignore non-alphanumeric)
   - Given a string `s`, check if it reads the same forward and backward after removing non-alphanumeric characters and ignoring case.

4. Find missing number in array from 1 to n
   - Given an array containing n-1 unique numbers in the range 1..n, find the missing number.
'''

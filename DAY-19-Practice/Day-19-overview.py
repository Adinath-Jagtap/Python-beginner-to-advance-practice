# =============================================================================
# PYTHON PROBLEMS (3)
# =============================================================================
'''
1. Merge two sorted arrays
   - Given two sorted arrays, merge them into a single sorted array.

2. Find the longest substring without repeating characters
   - Given a string, find the length (or substring) with all unique characters.

3. Rotate an array by k positions
   - Rotate the elements of an array to the right (or left) by k steps.
'''

# =============================================================================
# PYTHON FINAL PROBLEMS (3)
# =============================================================================
'''
1. Implement binary search
   - Given a sorted list `arr` and a target `x`, implement binary search to return the index of `x` or -1 if not found.

2. Find all pairs with given sum in array
   - Given a list `nums` and a target `t`, return all unique pairs (a, b) such that a + b == t.

3. Check if two strings are anagrams
   - Given two strings `s1` and `s2`, determine whether they are anagrams (ignore spaces/case if required).
'''